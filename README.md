# Microfrontend Demonstration Capability #1 Application

Run `npm run serve:single-spa` to start the dev server. This will start the server for this subapplication to listen at `http://localhost:4201/`.

